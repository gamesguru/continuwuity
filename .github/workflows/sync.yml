name: Sync

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      workflows: write
    steps:
      - name: Slim Checkout
        shell: bash
        run: |
          git clone --depth 1 ${{ github.server_url }}/${{ github.repository }}.git .
          git fetch --unshallow || true

      - name: Configure Git
        env:
          TOKEN: ${{ github.token }}
        run: |
          git config user.name "continuwuity CI"
          git config user.email "noreply@continuwuity.org"
          git config checkout.defaultRemote origin
          git config commit.gpgsign true

          # Re-configure origin with token for authenticated push
          SERVER_URL="${{ github.server_url }}"
          HOST="${SERVER_URL#https://}"
          git remote set-url origin https://x-access-token:$TOKEN@$HOST/${{ github.repository }}.git

          git remote add upstream https://forgejo.ellis.link/gamesguru/continuwuity.git

      - name: Sync main
        run: |
          echo "::group::Sync main"
          echo "Fetching from upstream..."
          if git fetch upstream main; then
            echo "Fetching from origin..."
            git fetch origin main || true
            if git show-ref --verify --quiet refs/remotes/origin/main; then
              echo "Checking out main..."
              git checkout -B main origin/main
              echo "Merging..."
              MERGE_OUTPUT=$(git merge FETCH_HEAD --no-edit)
              echo "$MERGE_OUTPUT"
              if [[ "$MERGE_OUTPUT" != "Already up to date." ]]; then
                echo "Pushing..."
                git push origin main --no-verify
              else
                echo "No changes to push."
              fi
            else
              echo "Creating main..."
              git checkout -b main FETCH_HEAD
              git push origin main --no-verify
            fi
            echo "Done."
          else
            echo "Failed to fetch upstream main."
            exit 1
          fi
          echo "::endgroup::"

      - name: Sync dev
        run: |
          echo "::group::Sync dev"
          echo "Fetching from upstream..."
          if git fetch upstream dev; then
            echo "Fetching from origin..."
            git fetch origin dev || true
            if git show-ref --verify --quiet refs/remotes/origin/dev; then
              echo "Checking out dev..."
              git checkout -B dev origin/dev
              echo "Merging..."
              MERGE_OUTPUT=$(git merge FETCH_HEAD --no-edit)
              echo "$MERGE_OUTPUT"
              if [[ "$MERGE_OUTPUT" != "Already up to date." ]]; then
                echo "Pushing..."
                git push origin dev --no-verify
              else
                echo "No changes to push."
              fi
            else
              echo "Creating dev..."
              git checkout -b dev FETCH_HEAD
              git push origin dev --no-verify
            fi
            echo "Done."
          else
            echo "Failed to fetch upstream dev."
            exit 1
          fi
          echo "::endgroup::"

      - name: Sync metadata
        run: |
          echo "::group::Sync metadata"
          echo "Fetching from upstream..."
          if git fetch upstream _metadata/badges; then
            echo "Fetching from origin..."
            git fetch origin _metadata/badges || true
            if git show-ref --verify --quiet refs/remotes/origin/_metadata/badges; then
              echo "Checking out metadata..."
              git checkout -B _metadata/badges origin/_metadata/badges
              echo "Merging..."
              MERGE_OUTPUT=$(git merge FETCH_HEAD --no-edit --allow-unrelated-histories)
              echo "$MERGE_OUTPUT"
              if [[ "$MERGE_OUTPUT" != "Already up to date." ]]; then
                echo "Pushing..."
                git push origin _metadata/badges --no-verify
              else
                echo "No changes to push."
              fi
            else
              echo "Creating metadata..."
              git checkout -b _metadata/badges FETCH_HEAD
              git push origin _metadata/badges --no-verify
            fi
            echo "Done."
          else
            echo "Upstream _metadata/badges not found, skipping."
          fi
          echo "::endgroup::"

      - name: Sync forge-head
        run: |
          echo "::group::Sync forge-head"
          echo "Fetching from upstream..."
          if git fetch upstream main; then
            echo "Updating forge-head branch..."
            git checkout -B _metadata/forge.ellis.link/HEAD FETCH_HEAD
            echo "Pushing..."
            git push origin _metadata/forge.ellis.link/HEAD --force --no-verify
            echo "Done."
          else
            echo "Failed to fetch upstream main for forge-head."
            exit 1
          fi
          echo "::endgroup::"
