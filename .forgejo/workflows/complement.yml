---
name: Complement Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  SKIP_CONFIRM: 1
  # Persistent CI cache directories on the host
  CARGO_HOME: /home/forgejo-runner/.cache/conduwuit-ci/cargo
  SCCACHE_DIR: /home/forgejo-runner/.cache/conduwuit-ci/sccache
  GOPATH: /home/forgejo-runner/.cache/conduwuit-ci/go
  RUSTC_WRAPPER: sccache
  PATH: /opt/rust/cargo/bin:${{ github.workspace }}/.cargo/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin

jobs:
  complement-tests:
    runs-on: ubuntu-24.04
    # Run directly on the VPS machine OS without a container block.
    # Eliminates container overhead and Docker-in-Docker.

    env:
      # Limit concurrent build jobs to avoid overloading the server
      CARGO_BUILD_JOBS: 2

    steps:
      - name: Slim Checkout
        run: |
          # Use raw git to avoid Node-based checkout action
          if [ ! -d ".git" ]; then
            git init
            git remote add origin ${{ forge.server_url }}/${{ forge.repository }}
          fi
          git fetch --depth 1 origin ${{ forge.sha }}
          git checkout FETCH_HEAD
          git fetch --tags --force

      - name: Native Rust Setup
        run: |
          # Use rustup to ensure toolchain is ready, but let it handle the file automatically
          rustup show
          mkdir -p "$CARGO_HOME" "$SCCACHE_DIR"

      - name: Build conduwuit for Complement
        run: PROFILE=dev SKIP_CONFIRM=1 CARGO_FLAGS="--features direct_tls" make build

      - name: Checkout Complement
        run: |
          # Manual checkout of complement to avoid Node overhead
          if [ ! -d "complement-src" ]; then
            git clone --depth 1 https://github.com/matrix-org/complement complement-src
          else
            cd complement-src && git fetch --depth 1 && git checkout FETCH_HEAD && cd ..
          fi

      - name: Build Complement Docker image
        run: docker build -t continuwuity:complement -f ./docker/complement.Dockerfile .

      - name: Build and run Complement tests
        continue-on-error: true
        run: |
          # Go installation should be present on the host or we can apt install it
          ./bin/complement ./complement-src

      - name: Summarize results
        id: summary
        if: always()
        run: |
          RESULTS="tests/test_results/complement/test_results.jsonl"
          if [ -f "$RESULTS" ]; then
            PASS=$(grep -c '"Action":"pass"' "$RESULTS" || true)
            FAIL=$(grep -c '"Action":"fail"' "$RESULTS" || true)
            SKIP=$(grep -c '"Action":"skip"' "$RESULTS" || true)
            TOTAL=$((PASS + FAIL + SKIP))
            echo "pass=$PASS" >> $GITHUB_OUTPUT
            echo "fail=$FAIL" >> $GITHUB_OUTPUT
            echo "skip=$SKIP" >> $GITHUB_OUTPUT
            echo "total=$TOTAL" >> $GITHUB_OUTPUT
          fi

      - name: Add PR comment
        if: ${{ forge.event_name == 'pull_request' && always() }}
        run: |
          PASS="${{ steps.summary.outputs.pass }}"
          FAIL="${{ steps.summary.outputs.fail }}"
          SKIP="${{ steps.summary.outputs.skip }}"
          TOTAL="${{ steps.summary.outputs.total }}"

          # Only post if we have results
          if [ -n "$TOTAL" ]; then
            BODY="Complement tests results: $PASS pass, $FAIL fail, $SKIP skip (total $TOTAL)"
            if [ "$FAIL" -gt 0 ]; then
              BODY="$BODY - ❌ Some tests failed!"
            else
              BODY="$BODY - ✅ All tests passed!"
            fi

            curl -X POST \
              -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
              -H "Content-Type: application/json" \
              -d "{\"body\": \"$BODY\"}" \
              "${{ forge.server_url }}/api/v1/repos/${{ forge.repository }}/issues/${{ forge.event.pull_request.number }}/comments"
          fi

      - name: Upload test logs
        if: always()
        uses: forgejo/upload-artifact@v4
        with:
          name: complement-logs
          path: tests/test_results/complement/
          retention-days: 90
